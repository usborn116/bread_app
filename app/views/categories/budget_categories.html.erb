<%= link_to "HOME", root_path %>

<h2>MONTHLY BUDGETS</h2>
<div id="budgets">
  <% @budgets.each do |category| %>
    <%= render category %>
    <p>
      <%= link_to "Show this category", category %>
    </p>
  <% end %>
</div>

<%= link_to "New category", new_category_path %>